#include <stdio.h>
int main(void)  // Void is replaced with void
{
    int sum;   // INT is replaced with int
        
    // COMPUTE RESULT
    sum = 25 + 37 - 19;     // added ";" at the end

    // DISPLAY RESULT

    printf ("The answer is %i\n", sum); // added "," after the string to set the variable sum
      
    return 0;

}

